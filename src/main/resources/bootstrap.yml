spring: 
  application: 
    name: zuul-service
  cloud:
    config:
        uri: ${CONFIG_SERVICE_URL}

security:
  jwt:
    secret: ${RPM_JWT_SECRET}
  zsign:
    secret: ${RPM_ZUUL_ACCESS_HEADER}
    salt: ${RPM_ZUUL_ACCESS_SALT}